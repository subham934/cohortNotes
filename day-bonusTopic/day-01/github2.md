what is git?

- track changes
- project manage karne main , in our system

Github is central codebase, where we put our code on cloud where anyone can access the code.

There are two types of repository:

1. Local
2. Remote


// Branching - Branching in GitHub is the practice of creating an isolated, parallel workspace within a repository to develop features, fix bugs, or experiment with code without altering the stable, main version of the project.

// Merging - Merging is the process of combining changes from two or more branches into a single branch, typically the main or master branch.


when a team works, nobody code in main branch. team members work on their own branch and name the branch. after the work is done on the branch ,we raise a request to merge the branch into main branch.


git branch xyz - create a branch name xyz
git switch xyz - switch to xyz branch
git branch - list all the branches
git checkout -b abc - creeate a new branch abc and switch to abc branch

