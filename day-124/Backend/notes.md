this is the copy of project121, where we have removed the testAi function from ai.service.js. now lets create another folder called Frontend.

after this we will create a Login.jsx file, for that we will use the useState and the Fetch API.


the useState will manage the state of the form, and the Fetch API will be used to send the form data to the backend.